# Day 4 學習日誌：Transform 層資料清洗與欄位驗證

今天主要強化 `taiwan-finance-data-pipeline` 專案中的 `transform.py`，重點是讓原始 CSV 資料在寫入 PostgreSQL 前，先經過資料清洗、欄位驗證與型別轉換。

今天完成的第一個重點是加入必要欄位檢查。當原始資料缺少必要欄位時，程式會直接提示缺少哪些欄位，而不是讓錯誤資料繼續往後面的 Load 階段流動。這樣可以在資料進入資料庫前先發現問題，降低髒資料進入資料庫的風險。

第二個重點是處理資料型別轉換。我將價格欄位轉成 numeric 型別，包含 `open_price`、`high_price`、`low_price`、`close_price`；同時也將 `volume` 轉成整數型別。這樣可以確保資料格式符合 PostgreSQL table schema，也方便後續做 SQL 查詢或分析。

第三個重點是清除數字欄位中的符號，例如逗號。金融資料中常會出現像 `1,000` 或 `25,000,000` 這種格式，如果沒有先清洗，可能會造成型別轉換失敗。因此在 Transform 階段先移除符號，再進行數字轉換。

第四個重點是處理日期欄位。我將 `trade_date` 轉成標準日期格式，確保資料可以正確寫入 PostgreSQL 的日期欄位。

最後，我也加入了空值處理。若 `stock_id` 或 `trade_date` 為空，該筆資料會被排除，因為這兩個欄位是判斷股票每日價格唯一性的核心欄位。如果這些欄位缺失，資料就不適合被寫入目標資料表。

今天學到的重點是：Transform 層不只是單純改欄位名稱，而是要負責資料進入資料庫前的品質控管。透過欄位檢查、型別轉換、符號清洗、日期處理與空值過濾，可以讓 ETL Pipeline 更穩定，也能避免不乾淨的資料進入 PostgreSQL。
