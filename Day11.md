# Day 11 學習筆記：使用 pytest 建立單元測試

今天開始為 Taiwan Finance Data Pipeline 加入自動化測試，使用 `pytest` 驗證日期處理與 TWSE 資料轉換的核心邏輯。

## 1. 安裝 pytest

在專案中加入 `pytest`，作為 Python 自動化測試工具。

現在可以在專案根目錄執行：

```bash
pytest
```

或查看更詳細的測試結果：

```bash
pytest -v
```

透過自動化測試，可以在修改程式後快速確認既有功能是否仍然正常。

## 2. 建立 tests 資料夾

新增 `tests` 資料夾，將測試程式與正式 ETL 程式分開管理。

目前測試主要涵蓋：

```text
日期處理邏輯
TWSE Transform 邏輯
```

這樣可以讓專案結構更清楚，也方便後續持續增加測試案例。

## 3. 測試合法交易日期

測試符合 `YYYYMMDD` 格式且真實存在的日期，例如：

```text
20240701
```

確認日期驗證函式可以正常接受並回傳合法日期。

需要注意的是，這項測試驗證的是「日期格式與日期本身是否合法」，不是確認該日期一定是台股交易日。

## 4. 測試不存在的日期

測試不存在的日期，例如：

```text
20240230
```

確認程式會擋下無效日期，而不會讓錯誤參數進入後續 ETL 流程。

這符合 Fail Fast 原則：

```text
先驗證輸入
→ 輸入正確後才執行 Pipeline
```

## 5. 測試日期區間產生函式

測試日期區間產生邏輯，例如輸入：

```text
開始日期：20240701
結束日期：20240703
```

預期產生：

```text
20240701
20240702
20240703
```

這可以確認日期區間包含開始日與結束日，並且會按照日期順序逐日產生。

## 6. 測試 TWSE 標準八欄轉換

建立小型測試 DataFrame，驗證 TWSE 中文欄位可以正確轉換成專案標準八欄 schema：

```text
stock_id
stock_name
trade_date
open_price
high_price
low_price
close_price
volume
```

這項測試可以確認 Transform 的欄位選取、重新命名及輸出順序符合預期。

## 7. 測試逗號數值轉換

測試 TWSE 原始資料中的逗號數值，例如：

```text
"25,000,000"
```

經過 Transform 後應轉換成：

```text
25000000
```

這可以確認資料中的千分位符號已被移除，而且欄位已轉換成可以運算的數值型別。

## 8. 測試 `"--"` 轉成缺失值

測試 TWSE 原始資料中的：

```text
"--"
```

經過數值轉換後，應變成：

```text
NaN
```

或：

```text
<NA>
```

這表示沒有有效數值的資料會被保留為缺失值，而不是錯誤地轉換成 `0`。

後續可以再由 Validation layer 根據資料品質規則決定如何處理。

## 9. 所有測試皆通過

執行 `pytest` 後，目前建立的測試案例全部通過。

這代表現階段的日期處理與 Transform 核心邏輯，皆符合測試中定義的預期結果。

## 今日理解

今天學習的重點不只是如何使用 `pytest`，而是如何將程式需求轉換成可以自動驗證的測試案例。

測試流程可以整理為：

```text
Arrange：準備輸入資料
→ Act：執行目標函式
→ Assert：驗證輸出結果
```

目前測試沒有呼叫真實 TWSE API，也沒有連線 PostgreSQL，而是使用固定的小型測試資料驗證純函式邏輯。

這可以讓測試具備以下特性：

- 執行速度快
- 結果穩定
- 不依賴網路
- 不依賴資料庫環境
- 發生錯誤時容易定位原因

這些測試也能作為 Regression Testing。未來修改 Transform、日期參數或加入 Airflow 後，可以重新執行 `pytest`，確認原本已完成的功能沒有被破壞。
